# Archived source snapshots

These files are historical rollback evidence for the Daily call widget. They are stored as `.dart.txt` so static analysis and production builds cannot treat them as active application source.

The authoritative implementation is `lib/custom_code/widgets/minimal_daily_widget.dart`. Restore an archive only through an explicit reviewed change followed by `flutter analyze` and `flutter test`.
